spring.application.name=OpenAI Prompt Tool Demo
server.port=8085

# Option 1: use environment variable OPENAI_API_KEY (recommended for Render)
# no need to put anything here.

# Option 2 (local only, NOT for GitHub):
# openai.api.key=sk-xxxx...
